table:
  name: groups
  schema: public
array_relationships:
  - name: contact_groups
    using:
      foreign_key_constraint_on:
        column: group_id
        table:
          name: contact_group
          schema: public
